% Discussion — outline only.
% Source: docs/paper_narrative_zh.md §8

\section{Discussion}
\label{sec:discussion}

\subsection{Why Does FT Direction Depend on Model $\times$ Dataset?}
% Hypothesis-level --- not proven, requires architectural ablation.
% - CBraMod criss-cross attention splits space and time; may engage
%   differently with ADFTD's delta-rich structure. Seed instability on
%   ADFTD hints at over-reliance on training trajectory.
% - REVE's linear patch embedding is scale-sensitive and window-sensitive.
% - LaBraM's neural tokenizer discretizes EEG before transformer; subtle
%   spectral differences in MDD (TDBRAIN) may be lost under FT.
% Flag this as open for future work.
TBD.

\subsection{Ruled-Out Alternatives}
% - Subject-adversarial GRL/DANN/LEAD (F12): $\lambda$ sweep 0.01--1.0,
%   all worse than baseline. Subject variance and label signal are
%   entangled in representation space.
% - Within-subject longitudinal DSS reframing (F14): all 3 FMs, 3
%   classifiers fail. Not salvageable.
% - Sparse-label-subspace hypothesis: directly refuted.
% - Spectral-guided FiLM: dropped from scope.
TBD.

\subsection{Clinical Implications (TNSRE Angle)}
% Frozen FM representations retain diagnostic value above classical
% (LaBraM 0.605 on Stress). But subject dominance means any clinical
% deployment requires per-individual calibration --- no generic model
% will generalize cold to new subjects.
% Personalized fine-tuning / few-shot adaptation is the pragmatic path.
% Requires larger datasets than 70 recordings to validate systematically;
% HNC (308+400 subjects, private Dementia+MDD) noted as potential
% high-powered follow-up.
TBD.

\subsection{Field-Level Recommendations}
% 1. Subject-level CV as default. Trial-level acceptable only for
%    literature comparison, with explicit leakage disclosure.
% 2. Statistical-power reporting: n\_subjects, n\_positive, multi-seed
%    std, permutation null distribution, bootstrap CI. Single-seed numbers
%    in small-sample regimes are not reproducible.
% 3. Window size is a first-class experimental factor; frozen/FT must be
%    window-matched.
TBD.
